app.controller("commusersettins", ["$scope", "MenuFactory", "$timeout", "$location", function ($scope, MenuFactory, $timeout, $location) {
    debugger;
    $scope.showhidesettings = function () {
        debugger;
        $scope.ishide = true;
    }

}]);